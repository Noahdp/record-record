# Record Record - Environment Configuration
# Copy this file to .env.local and update with your actual values

# =============================================================================
# DISCOGS API CONFIGURATION (REQUIRED)
# =============================================================================
# Get your credentials from: https://www.discogs.com/settings/developers
# 1. Create a new application
# 2. Copy your Consumer Key and Consumer Secret below
DISCOGS_CONSUMER_KEY=your_discogs_consumer_key_here
DISCOGS_CONSUMER_SECRET=your_discogs_consumer_secret_here

# =============================================================================
# DATABASE CONFIGURATION (OPTIONAL)
# =============================================================================
# SQLite database file location (default for development)
# For production, you can use PostgreSQL: postgresql://username:password@localhost:5432/database
DATABASE_URL=file:./prisma/dev.db

# =============================================================================
# APPLICATION CONFIGURATION (OPTIONAL)
# =============================================================================
# Environment mode - automatically set by hosting platforms
NODE_ENV=development

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# For Vercel deployment:
# 1. Add these environment variables in your Vercel dashboard
# 2. DATABASE_URL will be automatically configured if using Vercel Postgres
# 3. NODE_ENV will be automatically set to 'production'
#
# For other hosting platforms:
# 1. Ensure all required variables are set
# 2. Update DATABASE_URL for your production database
# 3. Set NODE_ENV=production for production builds
